%h1 Lists

- @lists.each do |list|
  .list_link
    %a{href: "/lists/#{list.id}"}
      = list.name
    
      %a{href: "/edit/#{list.id}"} 
        %button Edit
      %a{href: "/delete/#{list.id}"}
        %button Delete
%br
%a{href: "new"} Create a new list